import React from 'react';

/**
 * MCP (Model Context Protocol) is an open standard that enables AI models 
 * to securely connect to local and remote data sources and tools.
 * It provides a standardized way for AI assistants to access context 
 * (like files, databases, or API services) dynamically when needed.
 */
export const TestingMCP = () => {
  return (
    <div>
      <h1>Model Context Protocol (MCP)</h1>
      <p>
        MCP is an open standard that enables AI models to securely connect to local and remote data sources and tools.
      </p>
    </div>
  );
};

export default TestingMCP;
